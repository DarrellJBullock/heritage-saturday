# Heritage Saturday — local development infrastructure
# Scope: Capability 1 (roster import + basic game simulation) per company-docs/architecture.md.
#
# `docker compose up -d postgres redis` is the default dev stack and must never require a
# build. The `web` service is therefore behind a profile:
#
#   docker compose --profile web up --build web
#
# apps/api still runs on the host (`npm run start:dev`), so there is no `api` service —
# an apps/api Dockerfile is a separate follow-up, see docs/dev-setup.md.
#
# Cloud provider / IaC: none — this is local-only Docker Compose, not a cloud deployment.

services:
  postgres:
    image: postgres:16-alpine
    container_name: heritage-saturday-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: heritage
      POSTGRES_PASSWORD: heritage
      POSTGRES_DB: heritage_saturday
    ports:
      # Host port 5433, not 5432: many dev machines already run a local Postgres on 5432,
      # which silently shadows Docker's port-forward on 127.0.0.1/::1 (hit while standing
      # this up in this environment). Container-internal port stays 5432; only the host
      # mapping differs. Update DATABASE_URL if you change this.
      - "5433:5432"
    volumes:
      - heritage_saturday_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U heritage -d heritage_saturday"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  redis:
    image: redis:7-alpine
    container_name: heritage-saturday-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - heritage_saturday_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  web:
    # Behind a profile: `docker compose up -d postgres redis` must not try to build an image.
    profiles: ["web"]
    build:
      # Repo root, not apps/web — this is an npm-workspaces monorepo and the build needs
      # packages/shared plus the hoisted root node_modules.
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        # Baked into the client bundle at build time. The API's address is NOT here: the
        # browser calls this app's own /api/proxy/* route on the same origin.
        NEXT_PUBLIC_DEV_USER_ID: dev-user-1
    container_name: heritage-saturday-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      # Server-side fetches only (Server Components). Inside this container `localhost` is
      # the container, not the host — so SSR must reach the API explicitly. Defaults to the
      # host-run API. When running the `api` service too, point it at compose DNS:
      #   WEB_API_URL=http://api:3001 docker compose --profile api --profile web up --build
      # See apps/web/src/lib/config.ts.
      API_URL: ${WEB_API_URL:-http://host.docker.internal:3001}
      # Only needed if the API is running with API_SHARED_SECRET set. The default compose
      # `api` service opts out via ALLOW_INSECURE_NO_API_KEY, so this is normally empty.
      API_SHARED_SECRET: ${API_SHARED_SECRET:-}
    extra_hosts:
      # Docker Desktop provides host.docker.internal; on Linux this mapping creates it.
      # Only needed while the API runs on the host.
      - "host.docker.internal:host-gateway"

  api:
    # Behind a profile, like `web`: the default stack must not build an image. Running the
    # API on the host with `npm run start:dev` remains the normal dev loop; this service is
    # for exercising the container.
    profiles: ["api"]
    build:
      # Repo root: the API imports packages/{shared,importers,validation,simulation-engine}.
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: heritage-saturday-api
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      # Compose DNS, and the container-internal port 5432 — not the host's 5433 mapping.
      DATABASE_URL: postgresql://heritage:heritage@postgres:5432/heritage_saturday?schema=public
      REDIS_URL: redis://redis:6379
      # The image sets NODE_ENV=production, and the API refuses to start in production
      # without API_SHARED_SECRET. This is a local container on a private network, so opt
      # out explicitly. A real deployment sets the secret instead of this flag.
      ALLOW_INSECURE_NO_API_KEY: "true"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  heritage_saturday_postgres_data:
  heritage_saturday_redis_data:
